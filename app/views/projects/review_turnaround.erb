<%= render partial: 'shared/line-chart',
            locals: {
              metric_name: 'Review Turnaround',
              metric: @review_turnaround[:per_project]
            } if @review_turnaround && @review_turnaround[:per_project] %>