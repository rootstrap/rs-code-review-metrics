<%= render partial: 'shared/line-chart',
            locals: {
              metric_name: 'Average',
              metric: review_turnaround[:per_repository] || review_turnaround[:per_department]
            } if review_turnaround && (review_turnaround[:per_repository] || review_turnaround[:per_department]) %>
<%= render partial: 'shared/column-chart',
            locals: {
              chart_id: 'time-to-second-review',
              metric_name: 'Distribution',
              suffix: " reviews",
              metric: review_turnaround[:per_repository_distribution] || review_turnaround[:per_department_distribution]
            } if review_turnaround && (review_turnaround[:per_repository_distribution] || review_turnaround[:per_department_distribution]) %>
