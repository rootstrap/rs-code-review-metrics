<%= simple_form_for :metric, url: metrics_users_projects_path,
  method: 'GET', html: {
    class: 'nav-filter p-3 shadow-box sticky-top',
    id: 'nav-filter-form' } do |f|%>
  <%= yield f if block_given? %>
<% end %>
